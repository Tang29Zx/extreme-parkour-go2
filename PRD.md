# Extreme Parkour Go2 五箱能力测试 PRD

## 用户目标

使用 Extreme Parkour 的 Go2 预训练策略直接回放原 parkour 项目的五箱
赛道，先观察已有跨障碍能力，再决定是否设计专用奖励和训练流程。

## 验收标准

- 新任务名为 `go2_five_box`，不改变现有 `go2` 行为。
- 五个箱子的尺寸、高度和随机间距与原五箱配置一致。
- 四套 seed 0 布局可重复生成，箱体碰撞和高度扫描一致。
- `model_29500.pt` 无需参数迁移即可严格加载并产生动作。
- Docker CPU 少量环境完成创建和推理，无 NaN/Inf。

## 非目标

- 不实现逐箱通过判定、成功率、专用奖励或失败重置。
- 不扩展132点扫描，不修改 PPO、Actor 或 checkpoint。
- 不进行长时间训练。

## 风险与回滚

- 原策略未针对长箱体训练，回放可能无法通过；这是需要测量的能力结果，
  不是本阶段代码失败。
- 所有入口使用独立任务名；回滚只需移除新任务注册和五箱地形分支。

## 独立随机多箱 Sim-to-Real 任务

- 新任务名为 `go2_random_box`，不得改变 `go2` 或 `go2_five_box`。
- 使用 `10×40` 条可复现物理赛道，每条随机生成 2～6 个箱子；4096 个
  环境复用这 400 条布局。
- 箱体高度、长宽保持随机，横向偏移覆盖 `±1.0 m`，箱间距在
  `0.10～1.50 m` 内均匀采样。地面按30%平整、50%轻噪声和20%原仓库
  强噪声混合，避免鲁棒性训练抹掉平地步态。
- 第一版直接启用中等强度观测噪声、物理随机化、出生扰动和 0～40 ms
  动作延迟，但保持 Actor 观测维度、原生奖励和 PPO 不变。
- `model_29500.pt` 必须无需参数迁移即可严格加载；本阶段不解决真机 waypoint
  的生成问题。
- 新增 `go2_random_box_clean` 作为几何适应阶段：地形和物理范围与随机箱
  任务一致，但关闭Actor观测噪声、扫描安装抖动、接触丢失和动作延迟。

## 导出模型本地回放

### 用户目标

在不连接 Go2 和真实深度相机的情况下，使用 Docker 串联
`base_jit.pt` 与 `vision_weight.pt`，观看随机箱视觉策略的 Isaac Gym 回放。

### 验收标准

- `play_jit.py` 不再硬编码旧实验编号，可从指定目录加载标准命名的
  `base_jit.pt` 和 `vision_weight.pt`。
- 默认只创建 1 个环境，并允许使用现有 `--num_envs`、`--rows`、`--cols`
  和 `--random_box_layout` 参数覆盖。
- `go2_random_box` 与 `go2_random_box_clean` 保留各自随机箱地形，不被旧
  parkour 演示地形覆盖。
- 本地 RTX 5070 Ti 使用 CPU PhysX、CPU pipeline、CPU 相机回读完成视觉
  编码、策略推理和仿真步进。
- Viewer回放中按`R`重置当前观察环境，并同时清理episode状态与策略时序状态。
- 可选将 viewer 回放录制为 MP4；未启用录制时不产生额外文件。

### 风险、非目标与回滚

- CPU 回放可能低于实时速度，只用于功能检查，不作为训练性能基准。
- 回放不验证真机 DDS、关节映射、控制延迟或物理安全性。
- 不修改训练、蒸馏或 JIT 导出结构；回滚仅涉及回放脚本、公共参数和本节文档。

## 随机箱泛化评估场景

### 用户目标

使用一条训练集之外的固定长路线，同时评估Teacher和深度Student面对连续
10个箱子时的泛化能力，不改变训练使用的400条随机箱布局。

### 验收标准

- 独立任务名为`go2_random_box_eval`，默认仅创建1个环境和1条物理赛道；
  逻辑布局0为定制十箱路线，布局1精确复用训练分布中的原始布局130，
  布局2复用布局0，将箱长缩短`0.30 m`，并把箱宽、高度、常规箱间距分别
  限制在`0.90～1.40 m`、`0.20～0.50 m`、`0.20～0.70 m`；定点间距
  覆盖最高为`0.90 m`。
- 固定seed生成10个箱子；箱体高度、长宽继续使用训练任务范围。
- 横向偏移限制为`±0.5 m`，相邻箱间距为`0.3～1.0 m`。
- 第2→3、第4→5和第8→9箱间距固定为`0.35 m`，第4箱高度固定为
  `0.40 m`，用于重复验证短间距连续通过能力。
- 布局2将第3、6、10箱设为`0.45 m`高箱，第5、9箱设为`0.20 m`
  低箱，形成第2→3、第5→6、第7→8和第9→10箱四组低→高变化。
  高→低变化中，第1→2、第4→5、第6→7和第8→9箱前净空分别为
  `0.40/0.90/0.90/0.40 m`；第3→4、第7→8和第9→10箱间距为
  `0.25 m`。
- 布局2将第3箱相对布局0向机器人前进方向的右侧移动`0.40 m`，并将
  第5箱向右移动`0.20 m`。
- 布局3为超级难十箱路线：箱长`0.50～0.95 m`、箱宽`0.80～1.10 m`；
  高度依次为`0.20/0.50/0.15/0.48/0.20/0.50/0.15/0.45/0.15/0.50 m`，
  相邻箱持续形成低→高或高→低。
- 布局3横向中心在`-0.65～+0.65 m`间逐箱折返；第1→2至第9→10箱
  净空依次为`0.20/0.85/0.20/0.90/0.25/0.95/0.20/0.85/0.20 m`，
  地面粗糙度固定为`±0.06 m`。
- 布局0和布局2使用固定中等强度`±0.03 m`随机起伏；布局1保持源布局130
  的强粗糙度档，布局3使用`±0.06 m`。
- 赛道长度足以容纳最坏采样，并保留出口goal和末端余量。
- `--random_box_layout 0/1/2/3`能够单独选择四条路线；`go2_random_box`和
  `go2_random_box_clean`配置与生成结果不变。

### 非目标与回滚

- 该任务只用于回放评估，不参与训练、不新增奖励或成功判定。
- 不据单条路线结果替代完整测试集统计。
- 回滚时移除独立配置、任务注册及回放脚本中的任务名单即可。

## Go2 原生混合评估赛道

### 用户目标

通过独立任务 `go2_mixed` 在同一个 Viewer 中并排查看原仓库的五类 Go2
跑酷赛道、五条固定七箱赛道，以及从原400条训练布局中固定随机抽取的五条
赛道，不混入其他基础地形。

### 验收标准

- 地形固定为一行十五列：前五列顺序为
  `parkour / hurdle / step / gap / demo`，中间五列为可复现的七箱布局，
  最后五列以原训练布局 `51/186/357/130/221` 为前缀，并按原训练随机
  分布继续补齐为7个箱子。
- 原生赛道难度固定为 `1.0`；每条箱子赛道固定 7 个箱体，相邻箱间
  净空为 `0.20～1.00 m`、长宽各 `0.50～1.00 m`、高度 `0.20～0.50 m`、
  横向中心偏移 `-1.00～+1.00 m`。
- 所有十五条赛道均保持 8 个 waypoint 槽位；十条箱子赛道均为7个箱体中心
  加1个出口，不改变策略输入维度。
- 第3条新增箱子赛道的第1～4箱横向中心固定为
  `-0.25/+0.25/-0.25/+0.25 m`，相邻横移均为`0.50 m`。
- 默认创建 15 个环境，使每条物理赛道至少对应一只机器狗。
- `play.py` 和 `play_jit.py` 回放时保留该任务的十五列布局，不被通用回放
  地形覆盖。
- Viewer调试模式同时绘制15条赛道的全部waypoint；目标方向提示箭头仍只
  绘制当前`lookat_id`，避免自由视角被重复箭头遮挡。
- PPO训练episode日志统计完成最后一个waypoint的成功率：第1～5条汇总为
  原生赛道跳跃成功率，第6～10条汇总为定制七箱平均成功率。摔倒和自然超时
  计为失败，Viewer手动`R`重置不进入统计；第11～15条不进入这两个指标。
  不输出逐赛道明细。
- `go2`、`go2_five_box`、`go2_random_box*` 的配置与生成行为不变。

### 约束、非目标与回滚

- 原生五列复用原仓库地形生成器，不修改各原生障碍物的几何参数；十条箱子
  赛道复用现有随机箱生成器，不增加新的地形实现。
- 不新增奖励、观测或模型结构。
- 回滚时移除 `go2_mixed` 配置和注册，并删除地形固定难度的可选分支；
  未设置该选项的现有任务继续沿用原难度逻辑。

## 可复盘训练日志

### 用户目标

重新训练 Go2 策略时，无论是否启用 W&B，都在实验目录保留足以分析能力涌现、
定位训练退化并对应 checkpoint 的本地统计日志。

### 验收标准

- 每个训练 iteration 同时写入 TensorBoard 和长表 CSV；TensorBoard 不可用时
  训练继续，CSV 仍必须生成。
- 记录 PPO/视觉蒸馏 loss、学习率、动作噪声、总奖励、episode 长度、各奖励
  分量、吞吐和耗时。
- 所有基于 waypoint 的任务统一记录路线成功率、摔倒率、自然超时率、已完成
  waypoint 数及比例、episode 时长和水平位移。
- 按实际 terrain class 记录各类障碍的成功率；没有该类结束 episode 的周期
  不写伪造的零值。
- 每次启动追加保存命令行、任务配置、训练配置、Git commit 和 dirty 状态，
  不记录凭据或环境变量内容。
- checkpoint 内的 iteration 与文件名一致，并保存累计 timestep、累计训练时间
  和训练模式；另写不需要加载 PyTorch 文件即可查看的 checkpoint CSV 清单。
- 续训在同一实验目录追加日志，不覆盖已有记录；旧 checkpoint 缺少新元数据时
  使用可解释的回退值继续训练。

### 约束与非目标

- 日志只读取仿真中已经计算的 episode 状态，不修改奖励、终止条件、观测或
  策略网络。
- 本阶段提供按 terrain class 的障碍统计，不为400条随机箱布局分别创建指标，
  避免高基数日志；按箱距、高度差的离线分桶属于固定评估集任务。
- 回滚时可移除本地日志写入器和通用 episode 指标；checkpoint 权重格式继续
  兼容旧模型。

## Onboard 完整边界单箱评测

### 用户目标

使用 `/home/tang/Extreme-Parkour-Onboard/traced/` 当前成对导出权重，在本仓库
的 `0.25 m` 与 `0.45 m` 固定单箱场景中启用真实 Isaac 深度相机和 Sim-to-Real
随机化，并让每个 50 Hz 控制周期经过生产端同源的 Unitree LowState/LowCmd 边界、
策略接入保护和持续输出约束，量化成功率及动作被各类安全边界修改的周期数。

### 验收标准

- 每个高度默认执行20个独立随机 trial；固定seed用于复现，同一批环境仍分别采样
  出生位置/偏航/关节角、相机位置/姿态/FOV、深度噪声、摩擦、质量/质心、电机强度
  和0～2控制周期动作延迟。
- 单箱长宽均为`1.2 m`、助跑为`1.2 m`、地面粗糙度为`±0.005 m`；每局策略接入后
  最多运行15秒。
- 仿真必须执行L1接管检查、3秒站姿渐变、0.5秒策略prime、Y入口检查和1秒策略
  接入渐变；策略期持续执行动作`±4.8`裁剪、实测关节位置/速度检查、hip/thigh
  `0.20 rad`与calf `0.40 rad`周期目标限制、机械限位和PD力矩可行区间。
- 成功定义为依次完成箱体中心与箱后出口两个waypoint，且此前没有安全硬停；摔倒、
  超时、接入拒绝和安全硬停均计失败。
- JSON必须分别记录策略原始动作裁剪、接入保护修改、持续输出保护修改的控制周期，
  并将持续保护归因到目标步长、机械关节限位和预测PD力矩边界；同时保留每个trial的
  终止原因、策略周期数和随机化摘要。
- 权重、训练任务和生产安全模块不被复制或修改；评测入口从显式onboard仓库路径加载
  同源纯函数，并记录三个traced资产的SHA-256。

### 风险、非目标与回滚

- CPU相机回读只用于正确性和统计，不代表Jetson循环性能；Isaac Gym、随机化模型和
  固件PD仍不能完整代表真机动力学，因此结果不能单独授权真实电机接管。
- 运行期roll/pitch和足端接触按当前生产契约只记录，不新增自动motor-off；PhysX的
  摔倒终止属于任务失败，不冒充onboard安全限位触发。
- 不修改导出权重、任务奖励或真机入口。回滚只需删除联合评测脚本、对应参数/测试和
  本节文档，现有`play_jit.py`行为保持不变。
