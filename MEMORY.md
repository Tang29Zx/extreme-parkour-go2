# MEMORY.md

## 架构与运行环境

- 本地源码路径：`/home/tang/extreme-parkour-go2`。
- Docker 启动脚本位于 `/home/tang/parkour/docker/extreme_go2/`。
- 本地 RTX 5070 Ti 与仓库固定的 PyTorch 2.4.1 CUDA 12.4 不兼容
  `sm_120` 执行，因此本地回放使用 CPU PhysX、CPU pipeline 和 CPU RL。
- Go2 预训练模型位于
  `legged_gym/logs/parkour_A100/607-02-16/`；当前能力基线使用
  `model_29500.pt`。

## 已验证踩坑

- 原仓库基线提交内共跟踪149个`.pt`文件，约1.5GB，其中140个是训练
  checkpoint。主要有两套不同的teacher序列：
  `logs/extreme_parkour_a1_test/607-00-06`（87个checkpoint）和
  `logs/parkour_A100/607-02-16`（49个checkpoint）；两处
  `model_29500.pt`哈希不同，不能混用。多个目录中的`base_jit.pt`和
  `vision_weight.pt`内容完全相同，只是重复副本。
- 原仓库自带的`base_jit.pt`与`vision_weight.pt`是Go2视觉推理产物：
  配置使用Go2 URDF、53维本体观测、12维动作、87×58深度图、10步历史、
  `action_scale=0.25`和PD参数`Kp=40/Kd=1`。它们只能成对用于推理，
  不能恢复PPO训练；重复副本旁的相机配置并不完全一致，来源关系不够可靠，
  因此只能作为仿真基线，不能未经等价性和安全验证直接部署到真机。
- 仓库根目录现已忽略 Python/测试/IDE/本地编译缓存以及新生成的训练日志；
  `legged_gym` 源码目录中误提交的 `__pycache__/*.pyc` 已解除 Git 跟踪。
  Isaac Gym `_bindings/linux-x86_64/py36` 下随 SDK 分发的 `.pyc` 保持跟踪，
  不能用全仓库批量删除缓存的方式处理。
- `play.py` 原本硬编码作者机器日志路径，已改为相对
  `LEGGED_GYM_ROOT_DIR`。
- `train.py` 原本启动后等待 debugpy，已移除该阻塞。
- `OnPolicyRunner` 原本不更新或恢复 `current_learning_iteration`，导致每次续训
  都从 `model_0.pt` 重新编号并在结束时覆盖它。现在新适配任务可从本地0开始，
  checkpoint记录完成轮数；后续从该checkpoint恢复时继续编号和保存。
- 单环境回放可使用 `--rows 1 --cols 1`；单行地形的课程难度固定为0，避免
  原公式 `i / (num_rows - 1)` 触发除零。
- 随机箱回放支持 `--random_box_layout 0～399` 精确选择固定seed布局；综合
  6箱数量、箱高、横向折返、短间距和强地面粗糙度，当前最复杂候选为130号。
- `rsl_rl/modules/estimator.py` 的无用 turtle 导入会要求 Tk，已移除。
- Docker 脚本使用 `EXTREME_GO2_IMAGE_NAME`，避免被其他项目的
  `IMAGE_NAME` 环境变量覆盖。
- 原生策略没有“最后一个障碍跳跃”或显式 jump action；jump schedule 调用
  处于注释状态。跳跃是 PPO 在孤立踏石、深坑和跨栏地形上，受朝目标方向
  速度与朝向奖励驱动后学出的行为。当前五箱 waypoint 设在箱体中心且没有
  箱间平地 waypoint，因此从第四箱看第五箱时会直接复用跨越动作。

## 当前任务决策

- 2026-07-26：训练日志不再依赖 W&B。每个实验目录会生成 TensorBoard event、
  长表 `metrics.csv`、纯文本 `train.log`、`checkpoints.csv` 和追加式
  `run_manifest.jsonl`；`--no_wandb` 不影响这些本地资产。默认每 200 iteration
  保存一次 checkpoint，并同步写入 `checkpoints.csv`。
- Go2 训练的 PhysX `max_gpu_contact_pairs` 已提升到 `2**24`，为 3090 上测试
  8192～12288 个并行环境提供接触缓冲容量；该设置会增加 GPU 显存预留。
- 通用 waypoint episode 指标包含路线成功、摔倒、自然超时、goal 进度、
  episode 时长、水平位移和按 terrain class 的成功率；手动 Viewer reset
  不进入统计。PPO/视觉 loss、奖励分量、学习率、噪声和性能指标共用同一
  本地写入路径。
- 新 checkpoint 格式版本为2，保存正确 iteration、累计 timestep、累计训练
  时间和 RL/vision 模式；旧 checkpoint 缺少累计信息时按 iteration 和 rollout
  尺寸推算 timestep。1 iteration、64环境 CPU 冒烟已确认 TensorBoard 中
  产生44个 scalar，checkpoint 的 `iter=1`、`total_timesteps=1536`。

- 五箱能力测试保留 Extreme Parkour 原生132点扫描、Actor、PPO和奖励。
- 首版只迁移五箱地形与 waypoint，不迁移原 parkour 仓库的奖励、进度
  状态机、课程、KL或 checkpoint 操作器。
- 独立任务名为 `go2_five_box`；使用5个箱体中心和第五箱后平地作为
  6个顺序 waypoint，固定命令速度为 `0.5 m/s`。
- 已验证 `model_29500.pt` 可严格加载：4个CPU环境执行5步时观测形状
  为 `(4, 753)`、动作形状为 `(4, 12)`，观测和动作均为有限值。
- 能力回放中策略在原 `0.50 m` 第五箱稳定撞击；为单变量诊断，当前仅将
  第五箱降至 `0.40 m`，其他几何、扫描和命令保持不变。
- 后续观察确认失败发生在第四、第五箱之间，而非第五箱后的落地。原生目标
  推进只检查机身XY，不能据此认定已经稳定登上第五箱。默认第0环境的该段
  间距为 `1.40 m`，策略会强行直跳。短间距 `0.45～0.65 m` 会进一步匹配
  原生跳坑行为；当前诊断改用 `1.60～2.00 m` 的远间距，让第五箱在第四箱
  沿附近超出前向扫描范围，以测试策略能否先下地再接近。
- 独立 Sim-to-Real teacher 任务名为 `go2_random_box`：使用 `10×40=400`
  条固定种子布局，箱数2～6各80条，4096环境复用这些物理赛道；保留原生
  132点扫描、奖励、PPO和网络结构。该任务单独启用中等物理随机化、运行时
  Actor观测噪声及每局0～2控制步动作延迟，旧任务的新增开关默认关闭。
- 当前随机箱几何使用 `±1.0 m` 横向偏移和 `0.10～1.50 m` 均匀箱间距。
  400条赛道地面精确混合为30%平地、50% `0～0.02 m` 轻噪声和20%
  `0.02～0.06 m` 原仓库强噪声；类别固定seed打散。这些变化只作用于
  `go2_random_box`。
- `go2_random_box` 已用4个CPU环境严格加载 `model_29500.pt` 并连续执行5步，
  观测形状保持 `(4, 753)`，延迟0/1/2步均可采样；8环境检查确认摩擦、质量、
  质心和电机范围有效，噪声不产生NaN/Inf。相机噪声接口已配置，但teacher
  阶段仍使用高度扫描，深度渲染与蒸馏需在后续相机阶段单独验收。
- 几何适应任务 `go2_random_box_clean` 与完整随机箱任务共享几何和物理范围，
  但关闭Actor观测噪声、扫描偏移/抖动、接触丢失及动作延迟；用于从29500先
  训练300～500轮，再切换到完整Sim-to-Real随机化。
- 首次500轮 `random_box_clean_from29500` 是由修复checkpoint迭代记录之前的
  服务器进程生成；`model_0/100/200/300/400.pt`内部均记录iteration 0，
  其中修改时间最晚的`model_0.pt`是训练结束权重。切换到新run时从该文件加载，
  新版runner会重新从0正确编号。
- `random_box_sim2real_from30000` 已生成编号正确的30100～30500 checkpoint；
  全部模型及优化器张量有限。30500相对30000的Actor参数相对变化约3.0%，
  自适应学习率最终为`1e-5`。当前runner的TensorBoard writer被注释，使用
  `--no_wandb`时目录只保存checkpoint，不保存奖励、通过率或终止趋势；后续
  训练若需离线分析必须保留终端输出或恢复本地指标日志。
- 标准PPO和视觉蒸馏均固定每`50`个iteration保存一次checkpoint；续训时以
  本次恢复后的相对iteration计数触发，最终退出轮若不在50倍数上仍会额外保存。
- 400条 `25×4 m` 随机箱赛道若使用 `0.05 m` grid mesh，会生成约1768万
  顶点和3534万三角形，并在服务器 `gym.add_triangle_mesh()` 时触发段错误。
  两个随机箱任务因此改用仓库已有的 Delatin fast mesh，最大几何误差为
  `0.01 m`；原生任务继续使用原来的 grid mesh。

## 真机部署约束

- `model_29500.pt` 是使用仿真地形高度扫描点的 teacher policy，不能把该
  checkpoint 原样部署到 Go2；真机没有 Isaac Gym 的 heightfield 查询。
- 随机箱视觉蒸馏从noisy teacher `model_30500.pt`启动；相机模式默认使用
  192环境，每轮采集120步。首次加载时depth actor复制teacher Actor，teacher
  冻结，深度编码器和depth actor通过动作模仿与yaw损失更新。当前代码中的
  独立`depth_encoder_loss`被注释，因此显示0属预期；应监控depth actor loss、
  yaw loss、delta-yaw正确率和任务表现。使用`--no_wandb`时需用`tee`保存终端
  输出，否则没有可供后续分析的训练曲线。
- 服务器相机模式需要PhysX计算设备与Vulkan图形设备使用同一物理GPU；CUDA
  设备编号和Vulkan设备编号不保证一致。项目原先虽然解析
  `--graphics_device_id`，但没有传入环境，参数实际无效；现已接通并在
  `gym.create_sim()`前打印compute/graphics编号。多GPU相机模式应先用
  `vulkaninfo`确认Vulkan编号，再分别传`--device cuda:N`和
  `--graphics_device_id K`。随机箱相机地形的Delatin误差固定为`0.01 m`，
  防止继承原配置的`max_error_camera=2 m`后改变箱体几何。
- 多GPU相机训练不能在`CUDA_VISIBLE_DEVICES`只暴露一张卡后继续传物理
  `graphics_device_id>0`：Isaac Gym相机CUDA互操作会报
  `invalid device ordinal`。若CUDA与Vulkan编号一致，应暴露完整GPU列表，
  并让`--device cuda:N`、`--rl_device cuda:N`和
  `--graphics_device_id N`指向同一物理GPU。
- 192环境的随机箱视觉蒸馏进程在24GB GPU上实测占用约`16.5 GiB`（包含
  Isaac Gym非PyTorch显存）；如果同卡已有约`6.9 GiB`进程，会在首次深度卷积
  时OOM。该配置需要基本独占一张24GB卡；共享GPU时先确认进程归属，不能停止
  他人任务，必要时将相机环境数降至128。
- 相机深度后处理必须保持批量张量路径：Isaac Gym仍需逐传感器取得GPU tensor，
  但裁剪、高斯噪声、距离偏置、点丢失、随机遮挡、Bicubic缩放和历史buffer更新
  应在`torch.stack`后的全环境batch上执行，避免192环境逐张触发Python循环和
  GPU kernel。
- 推荐路径是先用最终 teacher policy 蒸馏得到深度相机 student，再导出匹配的
  base JIT 与 vision weights。teacher policy、视觉编码器与导出权重必须来自
  同一训练版本，不能把旧视觉权重和后续修改过的 Actor 混用。
- 本地单环境视觉回放支持显式传入 `--cpu_camera`：相机仍由NVIDIA/Vulkan
  渲染，但深度图通过Isaac Gym CPU readback读取，不经过
  `get_camera_image_gpu_tensor()`，可绕过本地PyTorch不支持RTX 5070 Ti
  `sm_120`的问题。该路径仅用于少量环境回放；服务器批量蒸馏继续使用默认
  GPU tensor相机路径。
- Viewer调试模式会绘制waypoint球体及当前/下一目标方向点；相机取图前会
  清理Viewer调试线，避免goal与脚部标记进入下一帧相机图像。无Viewer的
  `--headless`蒸馏行为不变。
- `go2_mixed`单独启用`draw_all_goals`，Viewer会同时绘制15条赛道的全部
  waypoint球体；方向提示箭头仍只绘制当前`lookat_id`。其他任务默认仍只
  绘制跟随环境的waypoint，避免增加通用Viewer开销。
- Viewer回放中按`R`会通过正常termination/reset链路重置当前观察环境，
  同时使视觉GRU收到done并清理episode、动作与深度历史；该快捷键不改变
  headless训练行为。
- 随机箱视觉任务已恢复原生`2.0 m`深度有效距离，并关闭`2.0～3.0 m`
  额外高斯噪声和无效点；两米以内既有相机噪声保持不变。通用远距离噪声
  实现仍保留但该任务不启用。深度GRU会按episode的`dones`逐环境清空，
  避免跨布局残留；`delta_yaw_ok`使用`abs(delta_yaw) < 0.6`，其余yaw
  蒸馏语义暂不改变。
- 独立泛化评估任务`go2_random_box_eval`使用固定未参与训练的seed
  `20260724`生成一条`30×4 m`十箱路线：横向偏移`±0.5 m`、箱间距
  `0.3～1.0 m`、地面粗糙度固定为`±0.03 m`。该任务默认仅1个环境和1条
  物理赛道，并关闭周期性水平推力，用于可复现地比较teacher/student；
  其他观测、相机和物理随机化仍保留，不修改`go2_random_box`、
  `go2_random_box_clean`或原生`go2`训练分布。评估路线的第2→3、第4→5
  和第8→9箱间距固定为`0.35 m`，第4箱高度固定为`0.40 m`。
  该任务另提供逻辑布局1，完整复用训练分布的原始布局130（6箱、强粗糙度档
  `0.02～0.06 m`）；逻辑布局2复用布局0的十箱序列，将每个箱体长宽各缩短
  `0.30 m`的初版已调整为：箱长仍精确缩短`0.30 m`，箱宽相较上一版逐箱
  增加`0.20 m`并限制在`0.90～1.40 m`，箱高限制在`0.20～0.50 m`，
  常规箱间净空限制在`0.20～0.70 m`，显式覆盖最高为`0.90 m`。
  布局2将第3、6、10箱设为`0.45 m`，第5、9箱设为
  `0.20 m`，形成四组低→高；第1→2、第4→5、第6→7和第8→9箱四组
  高→低变化前分别保留`0.40/0.90/0.90/0.40 m`净空。第3箱横向偏移从`+0.15 m`改为
  `-0.25 m`，即面对世界`+x`时向右移动`0.40 m`；第9→10箱净空为
  `0.25 m`。第5箱横向偏移从
  `+0.45 m`改为`+0.25 m`，即向右移动`0.20 m`。使用
  `--random_box_layout 0/1/2/3`在四条评估路线间切换。
- 逻辑布局3是超级难十箱路线：箱长`0.50～0.95 m`、箱宽
  `0.80～1.10 m`，高度按
  `0.20/0.50/0.15/0.48/0.20/0.50/0.15/0.45/0.15/0.50 m`
  高低交替；横向中心在`-0.65～+0.65 m`间逐箱折返，九段净空为
  `0.20/0.85/0.20/0.90/0.25/0.95/0.20/0.85/0.20 m`，地面粗糙度
  固定为`±0.06 m`。实际生成末箱后沿为`14.45 m`，出口goal为
  `15.45 m`，在30米赛道内保留充足末端余量。
- 独立评估任务`go2_mixed`使用`1×15`并排赛道：前五列为原仓库
  `parkour/hurdle/step/gap/demo`，中间五列为固定seed的随机七箱布局，
  最后五列以原400条训练路线中的布局`51/186/357/130/221`为前缀，并沿
  原训练seed随机序列补齐为7箱；原布局已有的`2/2/6/6/6`个箱子保持不变。
  前五列
  terrain type为`15/16/18/19/20`，其余十列均为`22`。七箱路线的箱间净空、长宽、
  高度和横向偏移范围分别为`0.20～1.00 m`、`0.50～1.00 m`、
  `0.20～0.50 m`和`±1.00 m`。默认15个环境；十条箱子列均使用7个箱体
  中心加1个出口，共8个waypoint，因此策略观测和checkpoint结构不变。
  第3条新增箱子赛道的第1～4箱横向中心为
  `-0.25/+0.25/-0.25/+0.25 m`，前三段相邻横移均为`0.50 m`；
  原生障碍难度固定为`1.0`，所有列地面粗糙度为`±0.02 m`；现有训练任务
  不设置固定难度字段，生成行为不变。`play.py`和`play_jit.py`均保留十五列布局。
- `go2_mixed`训练按完成最后一个waypoint统计episode成功率：物理列0～4汇总为
  `native_1_5_jump_success_rate`，列5～9汇总为
  `custom_6_10_average_success_rate`。两项写入PPO的`extras["episode"]`，在
  每个训练日志周期按结束episode取均值并进入终端与W&B；无样本周期跳过，
  不产生`NaN`。摔倒和自然超时计失败，手动`R`重置不计入分母，列10～14
  不纳入指标；回放脚本不输出累计或逐赛道成功率明细。
- 当前仓库有视觉蒸馏与 JIT 导出代码，但不包含完整、已验证的 Go2 真机低层
  控制程序；真机侧仍需适配 Unitree SDK2、D435i 深度输入、观测顺序、关节
  顺序/符号、动作缩放、默认姿态、PD 参数、控制频率、超时 watchdog 和急停。
- 上真机顺序必须是离线权重等价检查、只读 dry-run、悬挂/支架检查、平地低速
  测试、低矮单障碍测试，最后才是五箱；低层控制前必须避免与原生 sport mode
  同时发命令。
- `play_jit.py` 已改为从标准导出目录加载 `base_jit.pt` 与
  `vision_weight.pt`，默认单环境并保留随机箱任务地形；本地 Docker 使用
  `--device cpu --pipeline cpu --use_camera --cpu_camera` 可串联深度渲染、
  34维视觉输出、12维动作和 PhysX 回放。布局0已完成250步窗口与录制验证，
  生成的视频为1600×900、50 FPS、250帧，视觉和动作全程通过有限值检查。
- 2026-07-28 使用部署端完整边界保护（接入期`0.05 rad/周期`、稳态
  `0.10 rad/周期`、机械/速度/PD力矩交集）在CPU PhysX中比较视觉
  checkpoint。固定箱高`0.45 m`、地面粗糙度`±0.005 m`、seed 17、摩擦
  系数`1.0`时，`38700`在第513步无reset/无fault越过；`38300`因左后髋
  目标不可行停止，`39200`越箱时触发关节速度超限，训练曲线候选`39150`
  无fault但停在箱前。摩擦降至训练下界`0.7`后，已复测的三个导出候选均未
  安全通过：`38700`无fault但停在箱前，`38300`目标不可行，`39200`越箱时
  速度超限，因此当前相对最稳候选是`38700`，但不能据此宣称其能在低摩擦地面安全跨越
  `0.45 m`箱体。测试只在本地Docker执行，未同步权重或驱动真机。
- 2026-07-28 使用`model_30500.pt`的scandot teacher在CPU PhysX中完成固定
  `0.45 m`单箱摩擦压测：箱体长宽均为`1.2 m`、地面粗糙度`±0.005 m`，
  相机、推力、动作延迟和观测噪声关闭，质量/质心/电机固定为中性值；机器人
  刚体摩擦系数从`0.0`到`1.2`按`0.1`分档，每档16局，共208局全部完成箱体
  中心和箱后出口两个waypoint，无摔倒或超时，平均完成时间约`4.72～4.81 s`。
  地形网格摩擦系数仍为`1.0`，因此该结果验证的是现有domain randomization参数，
  不能解释为接触对总摩擦为零，也不包含部署端额外目标限幅。
- 2026-07-28 使用成对导出的`38700` base JIT与vision weights，在同一固定
  `0.45 m`单箱、机器人刚体摩擦系数`0.5`条件下通过CPU相机回读测试4局：
  3局成功、1局超时、无摔倒，三次成功用时`4.74/4.88/5.02 s`。测试不含
  部署端额外目标限幅；成功样本的第三人称回放位于
  `artifacts/model_38700_depth_box045_friction_0.5.mp4`。
- Go2相机配置中的`depth.position`是相对URDF根刚体`base`的仿真针孔相机
  光心，不是D435i外壳中心或安装孔。固定`go2`任务仍使用
  `(0.355, 0, 0.065) m`与向下`20～25°`；`go2_random_box`视觉蒸馏任务从
  2026-07-29起使用新均值`(0.355, 0, 0.115) m`，并保留位置标准差
  `(0.015, 0.010, 0.015) m`及roll/yaw `±2°`随机化。仓库
  `base.dae`的最大X约为`0.331958 m`，因此均值光心只在模型鼻端前方约
  `23 mm`，相对前髋轴平面向前`161.6 mm`。工程标注必须由URDF坐标直接
  投影，不能从生成式图片外观估点；实机仍需单独测量D435i光心到外壳/支架
  的偏置。
- 2026-07-28 将`model_38700.pt`视觉策略的相机位置均值从
  `(0.355, 0, 0.065) m`沿base Z上移`0.05 m`至`(0.355, 0, 0.115) m`，
  保留原位置标准差，在seed 17、刚体摩擦`0.5`、长宽`1.2 m`的固定单箱上
  各回放1局：`0.25 m`箱无摔倒但仅完成箱体目标、15.04秒超时；`0.45 m`
  箱在5.48秒完成两个目标且无摔倒。单seed不能解释为成功率；视频与配置摘要
  位于`artifacts/model_38700_depth_box0{25,45}_camera_zplus005_friction_0.5.mp4`
  和`artifacts/model_38700_depth_camera_zplus005_single_box_summary.json`；新旧光心
  对照标注图位于`artifacts/go2_camera_mount_zplus005_exact.{png,svg}`。
- 2026-07-29 已将`go2_random_box`仓库默认相机均值沿base Z上移`0.05 m`，
  用于从`model_38700.pt`继续视觉蒸馏；基础`go2`相机配置和已有checkpoint不变。
  续训应使用新的`exptid`并通过`resumeid=random_box_depth_from30500`加载旧目录，
  避免覆盖原`38750+`checkpoint。
- 2026-07-29 以旧光心`(0.355,0,0.065) m`、roll/yaw `0°`、pitch `22.5°`
  对`model_38700`视觉student完成单轴光心压力测试。固定地面粗糙度幅值
  `0.01 m`、刚体摩擦`0.7`、两步动作延迟及较强深度噪声；单箱通过定义为
  waypoint成功，或机身越过箱后1米出口平面且整局未摔倒。修正该判据是因为
  `0.25 m`策略会高速漏过出口waypoint，不能用waypoint超时直接判作没过箱。
  共完成1024局有效回放；保守稳定边界为X后移`10.0 cm`，即
  `(0.255,0,0.065) m`，后移`10.5 cm`在一个地形seed上降至`77.1%`；X前移
  `15 cm`与Y正向偏移`15 cm`仍分别达到四seed合计`80/86`和`86/86`，因此
  只证明上限至少15cm，未找到真正正向极限。结果是单轴测试，不能直接组合
  成二维矩形；详细口径在`docs/camera_position_stress_test.md`。
- 2026-07-29新增`evaluate_onboard_single_box.py`，从显式onboard仓库路径加载生产
  `joint_mapping/policy_context/real_control_safety/unitree_boundary`纯函数；Isaac真实
  深度相机、53维onboard本体观测、10帧历史、视觉GRU、LowState/LowCmd、L1后的3秒
  站姿、0.5秒prime、Y后1秒接入和持续输出约束均在同一50 Hz闭环中执行。固定单箱
  可保留出生、观测/深度、相机外参/FOV、摩擦、质量/质心、电机、推力和0～2周期
  动作延迟随机化；评测JSON区分actor `±4.8`裁剪与输出保护周期，并按目标步长、
  机械限位和PD力矩归因。
- 当前onboard `traced/`配套包哈希仍为`b556db.../897758.../4056bb...`，即
  `model_38300`。seed 17、每高度20局、长宽1.2 m、±5 mm地面、策略最长15秒的
  CPU相机评测中：0.25 m成功5/20（25%），8局安全硬停、7局超时；8799个策略周期
  中actor裁剪67周期，输出保护修改3402周期，其中持续约束2362周期，目标步长/
  机械/PD力矩边界分别命中1948/333/288周期（类别可重叠）。0.45 m成功4/20
  （20%），13局安全硬停、1局摔倒、2局超时；6348周期中actor裁剪750周期、输出
  保护修改3220周期，其中持续约束2180周期，三类边界分别命中1732/476/567周期。
- 0.25 m硬停为7次实测关节速度越界和1次RR calf联合可行区间为空；0.45 m为9次
  速度越界、2次RR thigh与1次RL thigh联合区间为空、1次实测关节位置越界。两批
  结果位于`artifacts/onboard_model_38300_box0{25,45}_trials20_seed17.json`；样本量
  和单seed不足以代表部署总体成功率，且CPU PhysX/相机不验证DDS、Jetson、固件PD
  或真机动力学，不能据此授权真实电机接管。
- 对同一两批闭环样本额外做只读稳态请求步长诊断，不计Y后首1秒接入阶段。按同类
  关节四条腿的最坏分位，0.25 m的95%分位为hip/thigh/calf约
  `0.076/0.300/0.263 rad`，99%分位约`0.118/0.412/0.416 rad`；0.45 m的95%
  分位约`0.096/0.440/0.413 rad`，99%分位约`0.212/0.797/0.619 rad`。若hip与
  thigh继续共用阈值，只以约95%的单关节请求不碰步长边界为目标，两种箱高约需
  `0.30/0.27 rad`和`0.44/0.42 rad`（hip-thigh/calf）；这只是当前受限轨迹的统计，
  不是放宽限位的安全推荐，放宽后必须重新闭环评测。
- 将生产边界hip/thigh调整到0.30 rad、calf保持0.40 rad后，按相同seed 17、每高度
  20局和完整随机化复测：0.25 m成功8/20（原5/20），3次安全硬停、9次超时；目标
  步长/持续约束/任意裁剪率分别为5.98%/11.81%/22.43%（原22.14%/26.84%/
  38.78%）。0.45 m成功3/20（原4/20），14次安全硬停、3次超时；三项率为
  8.71%/19.03%/36.86%（原27.28%/34.34%/52.69%）。高箱13次硬停来自实测速度
  越界，最大速度比例升至1.4706；减少目标步长裁剪没有转化为高箱成功率提升。
  新结果位于`artifacts/onboard_model_38300_step030_box0{25,45}_trials20_seed17.json`，
  旧0.20/0.40基线JSON保持不变。
- `evaluate_onboard_single_box.py`非headless模式现在按仿真帧率实时同步Isaac Viewer，
  `--viewer_env_id`可指定跟随的并行环境；headless评测保持原有快速执行。该入口只做
  仿真LowState/LowCmd闭环，不启动ROS、DDS或真机输出，也不自动录制视频。
  `--viewer_start_delay_s`可在播放前保持初始画面，`--viewer_hold_s`可在结束后保留
  最终画面，避免单环境因安全保护较快结束时窗口一闪而过。
- 0.25 m在相同`model_38300`、hip/thigh `0.30 rad`、calf `0.40 rad`和完整随机化
  条件下以独立seed 18复测20局：成功4局（20%）、安全硬停4局、接管前拒绝4局、
  超时8局；4次硬停均为calf实测速度越界（FL calf 3次、FR calf 1次），4次接管
  拒绝均为初始关节速度过高。回放所选env 11为FL calf负向速度`-21.803 rad/s`，
  相对`20.07 rad/s`名义限值为`1.08637×`。
  8289个策略周期中任意裁剪1904周期（22.97%），目标步长650周期（7.84%）、
  持续约束1052周期（12.69%）、机械限位288周期（3.47%）、PD力矩192周期
  （2.32%）。与seed 17合并为40局12次成功（30%），说明单批8/20的40%存在明显
  随机波动；合并18170周期的任意裁剪率为22.67%。新结果位于
  `artifacts/onboard_model_38300_step030_box025_trials20_seed18.json`。
- 当前Go2训练URDF的calf `velocity=20.07 rad/s`、`effort=35.55 Nm`来自Unitree
  官方Go2 URDF的早期值；Unitree官方仓库在2023-10-11提交`79f054e`中将四个
  knee/calf修正为`15.70 rad/s`和`45.43 Nm`。本仓库URDF自2025-06-06初始导入
  起仍保留修正前数值，Onboard边界随后从该本地URDF复制，因此`20.07`不是从真机
  固件实时读取或按具体Go2硬件版本实测得到。未核对机器人具体版本、固件与厂家
  资料前，不得仅依据任一URDF自动改动部署速度/扭矩边界。
- 2026-07-29 已从`random_box_depth_from40750_05`成对导出`model_41500`、
  `model_41650`和`model_41850`，标准包位于
  `legged_gym/logs/box_parkour/exports/model_<checkpoint>/`，每包包含
  `base_jit.pt`、`vision_weight.pt`和`config.json`。逐张量配对、CPU JIT
  载入、753维观测到12维有限动作及相机光心均已验证；三份配置均为
  `(0.355, 0, 0.115) m`。当前优先评测`41650`，`41500`作为备选，
  `41850`作为低loss对照。
- 2026-07-29 已为`model_41650`录制固定`0.25/0.45 m`单箱回放，条件为
  箱体长宽`1.2 m`、地面粗糙度`±0.005 m`、刚体摩擦`0.5`、seed 17、
  相机均值`(0.355,0,0.115) m`，并关闭额外观测噪声、推力和动作延迟。
  视频位于`artifacts/model_41650_depth_box0{25,45}_camera_z0115_friction_0.5.mp4`；
  `0.25 m`视频为264帧/5.28秒，但该次终端未保留episode结果，不据此标记成功；
  `0.45 m`为751帧/15.02秒，episode结果为完成1个goal、无摔倒、超时。
- 2026-07-29 用户确认D435i为正装。`go2_random_box`后续视觉蒸馏的相机均值
  从`(0.355,0,0.115) m`改为支架与模型反推的深度光心
  `(0.344,+0.0175,0.080) m`；正装时左红外镜头位于Go2 `base +Y`侧。
  位置标准差仍为`(0.015,0.010,0.015) m`，姿态仍为roll/yaw `±2°`、pitch
  `20～25°`。该位置是厘米级工程估计，实机测量后仍应校准；已有checkpoint
  和导出包保留各自训练时的`(0.355,0,0.115) m`配置，不追溯改写。
